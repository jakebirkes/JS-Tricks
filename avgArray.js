// let exArr = [1, 2, 3, 4, 5, 6];

function avgArray(arr) {
    var s = 0;
    for (var i = 0; i < arr.length; i++) {
      s = s + arr[i]
    }
    avg = s / arr.length
    return avg;
  }

// avgArray(exArr);
  
/* 

> 3.5

*/